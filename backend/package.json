{
  "name": "pbl_bus_backend",
  "version": "0.1.0",
  "scripts": {
    "start": "cross-env NODE_ENV=development nodemon --watch 'src/**/*.ts' --exec ts-node src/server.ts",
    "prod": "cross-env NODE_ENV=production nodemon --watch 'src/**/*.ts' --exec ts-node src/server.ts"
  },
  "dependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.27",
    "@types/pg": "^8.11.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "pg": "^8.11.3",
    "typescript": "^5.4.2"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2"
  }
}
